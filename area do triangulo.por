programa {
  funcao inicio() {
  real base, altura, area
  escreva ("insira a base:")
  leia (base)
  escreva ("insira a altura:")
  leia (altura)
area = ((base * altura) / 2)
escreva(" a area do triangulo é:", area)
  }
}
